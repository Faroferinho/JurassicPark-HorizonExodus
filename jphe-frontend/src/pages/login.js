import React from 'react';

const Login = () => {
    return (
        <div>
        <h1>Login Page</h1>
        <p>This is the login page where users can enter their credentials to access the system.</p>
        {/* Login form can be added here */}
        </div>
    );
}
export default Login;